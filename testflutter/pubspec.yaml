name: testflutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  http: ^1.2.2
  get_it: ^7.7.0
  shared_preferences: ^2.3.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.2.4  # Bài 5 — lưu JWT token an toàn (encrypted)
  hydrated_bloc: ^9.1.5           # Tuần 2 Ngày 1 — tự động lưu/khôi phục state
  path_provider: ^2.1.0           # Tuần 2 Ngày 1 — lấy đường dẫn thư mục app
  go_router: ^14.0.0              # Tuần 2 Ngày 2 — declarative navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  bloc_test: ^9.1.7
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
